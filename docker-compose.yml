services:
  postgres:
    image: postgres:16-alpine
    container_name: travel-app-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: travel_app
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - travel-app-network

  api:
    build:
      context: ./packages/api-go
      dockerfile: Dockerfile.dev
    container_name: travel-app-api
    volumes:
      - ./packages/api-go:/app
      - /app/tmp # Exclude tmp directory from sync
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USER: postgres
      DB_PASSWORD: postgres
      DB_NAME: travel_app
      GIN_MODE: debug # debug mode for local development
      SEED_DATA: "true"
      # CORS_ALLOWED_ORIGINS: "http://localhost:3000,http://localhost:4200"  # Comma-separated list
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - travel-app-network
    command: air -c .air.toml

volumes:
  postgres_data:

networks:
  travel-app-network:
    driver: bridge
