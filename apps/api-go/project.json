{
  "name": "api-go",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/api-go",
  "projectType": "application",
  "tags": ["type:api", "lang:go"],
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "go run main.go",
        "cwd": "apps/api-go"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "go build -o dist/api-go main.go",
        "cwd": "apps/api-go"
      },
      "outputs": ["{workspaceRoot}/apps/api-go/dist"]
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "go test ./...",
        "cwd": "apps/api-go"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "go vet ./...",
        "cwd": "apps/api-go"
      }
    },
    "tidy": {
      "executor": "nx:run-commands",
      "options": {
        "command": "go mod tidy",
        "cwd": "apps/api-go"
      }
    },
    "generate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "go run github.com/99designs/gqlgen generate",
        "cwd": "apps/api-go"
      }
    },
    "seed": {
      "executor": "nx:run-commands",
      "options": {
        "command": "go run cmd/seed/main.go",
        "cwd": "apps/api-go"
      }
    }
  }
}
