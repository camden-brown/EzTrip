# GraphQL Schema

type User {
  id: ID!
  firstName: String!
  lastName: String!
  email: String!
}

type Query {
  # Returns the currently authenticated user.
  # For now this is a placeholder until JWT auth is implemented.
  currentUser: User
  users: [User!]!
  user(id: ID!): User
}
