# Development Dockerfile with hot reload support
FROM golang:1.24-alpine

WORKDIR /app

# Install Air for hot reload (use specific version compatible with Go 1.24)
RUN go install github.com/air-verse/air@v1.52.3

# Copy go mod files
COPY go.mod go.sum ./
RUN go mod download

# Copy source code (will be overridden by volume mount in docker-compose.override.yml)
COPY . .

# Expose port
EXPOSE 8080

# Air will be started by docker-compose command
CMD ["air", "-c", ".air.toml"]
